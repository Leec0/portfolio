---
import Button from "../pageElements/Button.astro";
import Card from "../pageElements/Card.astro";
const {title, description, date, slug} = Astro.props;
---
<Card>
    <section class="flex flex-col justify-between h-full gap-4">
        <section class="flex flex-col gap-1">
            <section class="flex flex-row justify-between items-center">
                <h3 class="text-xl font-semibold text-text">{title}</h3>
                <p class="text-text-muted text-sm">{date}</p>
            </section>
            <p class="text-text-muted">{description}</p>
        </section>
        <Button size="default" href=`/projects/${slug}`>More details</Button>
    </section>
</Card>